package enums;

/* **************
 * Enum: Operator
 * **************
 * Description:	An enum representing the operators that can be performed
 * 				on a node. Values are self-explanatory.
 */
public enum Operator {
	UP,
	DOWN,
	LEFT,
	RIGHT
}
